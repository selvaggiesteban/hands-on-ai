{
  "category": "JavaScript",
  "topic": "ES6 Modules",
  "items": [
    "Are 'import' and 'export' statements used at the top level?",
    "Are named exports preferred over default exports for refactoring safety?",
    "Are file extensions included in imports if required (e.g., in browser native modules)?",
    "Are circular dependencies avoided?",
    "Is 'export *' avoided to prevent namespace pollution?",
    "Are dynamic imports ('import()') used for code splitting?"
  ]
}